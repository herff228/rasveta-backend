const axios = require('axios');

const generateAllTasks = async () => {
  try {
    console.log('🔄 Генерирую 9 заданий с названиями...');
    
    const response = await axios.post(
      'https://api.mistral.ai/v1/chat/completions',
      {
        model: 'mistral-small-latest',
        messages: [
          {
            role: 'user',
            content: `Придумай 9 разных заданий для саморазвития (по одному на уровень с 1 по 9). 
Для каждого задания придумай КОРОТКОЕ НАЗВАНИЕ (2-4 слова) и само задание (описание).

Формат ответа строго:
Название 1: [название]
Задание 1: [описание]

Название 2: [название]
Задание 2: [описание]

И так до 9.

Пример:
Название 1: Утренняя прогулка
Задание 1: погуляй в парке 30 минут, слушая пение птиц

Название 2: Час с книгой
Задание 2: прочитай 20 страниц любой книги в тишине

Название 3: Творческий вечер
Задание 3: нарисуй пейзаж акварелью или карандашами

Название 4: Кулинарный эксперимент
Задание 4: приготовь новое блюдо по рецепту из интернета

Название 5: Зарядка для тела
Задание 5: сделай 20 приседаний, 15 отжиманий, планка 2 минуты

Название 6: Звонок близким
Задание 6: позвони другу или родственнику, с которым давно не общался

Название 7: Прогулка на природе
Задание 7: сходи в лес или парк, собери букет из листьев

Название 8: Культурный выход
Задание 8: посети музей, выставку или галерею

Название 9: Планирование будущего
Задание 9: напиши подробный план на следующий месяц`
          }
        ],
        temperature: 0.9,
        max_tokens: 800
      },
      {
        headers: {
          'Authorization': `Bearer ${process.env.MISTRAL_API_KEY}`,
          'Content-Type': 'application/json'
        }
      }
    );
    
    const content = response.data.choices[0].message.content;
    console.log('✅ Ответ от Mistral получен');
    console.log('📝 Ответ:', content);
    
    const tasks = [];
    const lines = content.split('\n').filter(line => line.trim() !== '');
    
    for (let i = 1; i <= 9; i++) {
      let titleLine = lines.find(l => l.includes(`Название ${i}:`)) || 
                      lines.find(l => l.toLowerCase().includes(`название ${i}`));
      let taskLine = lines.find(l => l.includes(`Задание ${i}:`)) || 
                     lines.find(l => l.toLowerCase().includes(`задание ${i}`));
      
      let title = '';
      let task = '';
      
      if (titleLine) {
        title = titleLine.split(':')[1]?.trim() || '';
        title = title.replace(/\*\*/g, '').replace(/\*/g, '').replace(/_/g, '').trim();
      }
      
      if (taskLine) {
        task = taskLine.split(':')[1]?.trim() || '';
        task = task.replace(/\*\*/g, '').replace(/\*/g, '').replace(/_/g, '').trim();
      }
      
      if (!title) title = getDefaultTitle(i);
      if (!task) task = getDefaultTask(i);
      
      tasks.push({ title, task });
    }
    
    console.log('✅ Задания с названиями сгенерированы:');
    tasks.forEach((t, i) => console.log(`   Уровень ${i+1}: ${t.title} — ${t.task}`));
    
    return tasks;
    
  } catch (error) {
    console.error('❌ Ошибка генерации через Mistral:', error.response?.data || error.message);
    console.log('🔄 Использую задания по умолчанию');
    return getDefaultTasks();
  }
};

const getDefaultTitle = (level) => {
  const titles = {
    1: "Утренняя прогулка",
    2: "Час с книгой",
    3: "Творческий вечер",
    4: "Кулинарный эксперимент",
    5: "Зарядка для тела",
    6: "Звонок близким",
    7: "Прогулка на природе",
    8: "Культурный выход",
    9: "Планирование будущего"
  };
  return titles[level] || `Уровень ${level}`;
};

const getDefaultTask = (level) => {
  const tasks = {
    1: "погуляй в парке 30 минут, слушая пение птиц",
    2: "прочитай 20 страниц любой книги в тишине",
    3: "нарисуй пейзаж акварелью или карандашами",
    4: "приготовь новое блюдо по рецепту из интернета",
    5: "сделай 20 приседаний, 15 отжиманий, планка 2 минуты",
    6: "позвони другу или родственнику, с которым давно не общался",
    7: "сходи в лес или парк, собери букет из листьев",
    8: "посети музей, выставку или галерею",
    9: "напиши подробный план на следующий месяц"
  };
  return tasks[level] || tasks[1];
};

const getDefaultTasks = () => {
  return [
    { title: "Утренняя прогулка", task: "погуляй в парке 30 минут, слушая пение птиц" },
    { title: "Час с книгой", task: "прочитай 20 страниц любой книги в тишине" },
    { title: "Творческий вечер", task: "нарисуй пейзаж акварелью или карандашами" },
    { title: "Кулинарный эксперимент", task: "приготовь новое блюдо по рецепту из интернета" },
    { title: "Зарядка для тела", task: "сделай 20 приседаний, 15 отжиманий, планка 2 минуты" },
    { title: "Звонок близким", task: "позвони другу или родственнику, с которым давно не общался" },
    { title: "Прогулка на природе", task: "сходи в лес или парк, собери букет из листьев" },
    { title: "Культурный выход", task: "посети музей, выставку или галерею" },
    { title: "Планирование будущего", task: "напиши подробный план на следующий месяц" }
  ];
};

module.exports = { generateAllTasks };
